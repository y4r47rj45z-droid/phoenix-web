const menuBtn=document.getElementById('menuBtn');const mobileNav=document.getElementById('mobileNav');if(menuBtn&&mobileNav){menuBtn.addEventListener('click',()=>mobileNav.classList.toggle('show'));mobileNav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mobileNav.classList.remove('show')))}
const cookie=document.getElementById('cookie');const accept=document.getElementById('acceptCookie');if(cookie&&!localStorage.getItem('phoenix_cookie_ok')) cookie.classList.add('show');if(accept) accept.addEventListener('click',()=>{localStorage.setItem('phoenix_cookie_ok','1');cookie.classList.remove('show')});
